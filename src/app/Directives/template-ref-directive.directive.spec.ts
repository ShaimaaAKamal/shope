import { TemplateRefDirectiveDirective } from './template-ref-directive.directive';

describe('TemplateRefDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateRefDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
